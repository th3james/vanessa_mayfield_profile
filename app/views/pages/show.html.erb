<p id="notice"><%= notice %></p>

<h2><%= @page.title %></h2>

<p><%= RedCloth.new(@page.body).to_html.html_safe %></p>

<% if can? :edit, @page %>
  <%= link_to 'Edit', edit_page_path(@page) %> |
  <%= link_to 'View Pages', pages_path %>
<% end %>
